# Aura UI 完整 API 文档

## Button
Props:
- variant: 'default' | 'primary' | 'dashed' | 'text' | 'link' （默认 'default'）
- size: 'sm' | 'md' | 'lg' （默认 'md'）
- disabled: boolean
- loading: boolean
- className: string
- style: CSSProperties
- children: ReactNode
继承 ButtonHTMLAttributes<HTMLButtonElement>
Usage:
import { Button } from '@aura/ui';
<Button variant="primary" size="md">按钮</Button>
<Button loading>加载中</Button>
<Button variant="dashed" disabled>禁用</Button>

## Typography
### Title
Props:
- level: 1 | 2 | 3 | 4 | 5 （默认 1）
继承 HTMLAttributes<HTMLHeadingElement>
Usage:
import { Typography } from '@aura/ui';
<Typography.Title level={2}>标题</Typography.Title>
### Text
Props:
- variant: 'default' | 'secondary' | 'success' | 'warning' | 'danger'
- strong: boolean
- underline: boolean
- delete: boolean
- code: boolean
- mark: boolean
继承 HTMLAttributes<HTMLSpanElement>
Usage:
<Typography.Text variant="secondary">次要文字</Typography.Text>
<Typography.Text strong bold>加粗文字</Typography.Text>
### Paragraph
Props:
- ellipsis: boolean
继承 HTMLAttributes<HTMLParagraphElement>
Usage:
<Typography.Paragraph ellipsis>长文本内容...</Typography.Paragraph>

## Space
Props:
- direction: 'horizontal' | 'vertical' （默认 'horizontal'）
- size: 'sm' | 'md' | 'lg' | number （默认 'md'）
- wrap: boolean
- align: 'start' | 'center' | 'end' | 'baseline'
继承 HTMLAttributes<HTMLDivElement>
Usage:
import { Space } from '@aura/ui';
<Space direction="horizontal" size="md">
  <Button>按钮1</Button>
  <Button>按钮2</Button>
</Space>

## Divider
Props:
- direction: 'horizontal' | 'vertical' （默认 'horizontal'）
- variant: 'default' | 'dashed' （默认 'default'）
- orientation: 'left' | 'center' | 'right' （默认 'center'）
继承 HTMLAttributes<HTMLDivElement>
Usage:
import { Divider } from '@aura/ui';
<Divider />
<Divider variant="dashed" orientation="left">标题</Divider>

## Input
Props:
- variant: 'default' | 'filled' | 'bordered' （默认 'default'）
- size: 'sm' | 'md' | 'lg' （默认 'md'）
- disabled: boolean
- placeholder: string
- value: string
- onChange: (e: ChangeEvent) => void
- prefix: ReactNode
- suffix: ReactNode
- allowClear: boolean
- status: 'default' | 'error' | 'warning'
继承 InputHTMLAttributes（排除 size、prefix）
子组件: Input.Password, Input.Search, Input.Group
### Input.Password
继承 InputProps（排除 type），额外：
- visibilityToggle: boolean
### Input.Search
继承 InputProps（排除 suffix），额外：
- onSearch: (value: string) => void
- searchButton: boolean
### Input.Group
Props:
- compact: boolean
继承 HTMLAttributes<HTMLDivElement>
Usage:
import { Input } from '@aura/ui';
<Input placeholder="请输入" />
<Input.Password placeholder="密码" />
<Input.Search placeholder="搜索" onSearch={handleSearch} />

## Textarea
Props:
- variant: 'default' | 'filled' | 'bordered' （默认 'default'）
- size: 'sm' | 'md' | 'lg' （默认 'md'）
- autoSize: boolean | { minRows: number; maxRows: number }
- showCount: boolean
- maxLength: number
- status: 'default' | 'error' | 'warning'
继承 TextareaHTMLAttributes
Usage:
import { Textarea } from '@aura/ui';
<Textarea placeholder="请输入" />
<Textarea autoSize={{ minRows: 2, maxRows: 6 }} showCount maxLength={200} />

## Select
Props:
- variant: 'default' | 'filled' | 'bordered' （默认 'default'）
- size: 'sm' | 'md' | 'lg' （默认 'md'）
- disabled: boolean
- loading: boolean
- options: Array<{ label: ReactNode; value: string | number; disabled?: boolean }>
- value: string | number | (string | number)[]
- defaultValue: string | number | (string | number)[]
- multiple: boolean
- searchable: boolean
- clearable: boolean
- placeholder: string （默认 '请选择'）
- onChange: (value: string | number | (string | number)[]) => void
- className: string
- style: CSSProperties
Usage:
import { Select } from '@aura/ui';
<Select options={[{ value: 'a', label: '选项A' }]} onChange={handleChange} />
<Select multiple searchable clearable options={options} />

## Checkbox
Props:
- size: 'sm' | 'md' | 'lg' （默认 'md'）
- indeterminate: boolean
继承 InputHTMLAttributes（排除 size）
### CheckboxGroup
Props:
- value: (string | number)[]
- defaultValue: (string | number)[]
- options: Array<{ label: ReactNode; value: string | number; disabled?: boolean }>
- onChange: (value: (string | number)[]) => void
- direction: 'horizontal' | 'vertical' （默认 'horizontal'）
- disabled: boolean
- size: 'sm' | 'md' | 'lg'
Usage:
import { Checkbox } from '@aura/ui';
<Checkbox>选项</Checkbox>
<Checkbox.Group options={options} onChange={handleChange} />

## Radio
Props:
- size: 'sm' | 'md' | 'lg' （默认 'md'）
继承 InputHTMLAttributes（排除 size）
### RadioGroup
Props:
- value: string | number
- defaultValue: string | number
- options: Array<{ label: ReactNode; value: string | number; disabled?: boolean }>
- onChange: (value: string | number) => void
- direction: 'horizontal' | 'vertical' （默认 'horizontal'）
- disabled: boolean
- size: 'sm' | 'md' | 'lg'
Usage:
import { Radio } from '@aura/ui';
<Radio>选项</Radio>
<Radio.Group options={options} onChange={handleChange} />

## Switch
Props:
- size: 'sm' | 'md' | 'lg' （默认 'md'）
- disabled: boolean
- loading: boolean
- checked: boolean
- defaultChecked: boolean
- checkedChildren: ReactNode
- unCheckedChildren: ReactNode
- onChange: (checked: boolean) => void
- className: string
- style: CSSProperties
Usage:
import { Switch } from '@aura/ui';
<Switch onChange={handleChange} />
<Switch checkedChildren="开" unCheckedChildren="关" />

## Alert
Props:
- variant: 'default' | 'success' | 'warning' | 'error' | 'info' （默认 'default'）
- title: ReactNode
- closable: boolean
- showIcon: boolean
- onClose: () => void
- className: string
- style: CSSProperties
- children: ReactNode（描述内容）
Usage:
import { Alert } from '@aura/ui';
<Alert variant="success" title="成功">操作已完成</Alert>
<Alert variant="error" showIcon closable>出错了</Alert>

## Spin
Props:
- size: 'sm' | 'md' | 'lg' （默认 'md'）
- spinning: boolean （默认 true）
- tip: ReactNode
- indicator: ReactNode
- children: ReactNode
- className: string
- style: CSSProperties
Usage:
import { Spin } from '@aura/ui';
<Spin spinning={loading}>
  <Content />
</Spin>
<Spin size="lg" tip="加载中..." />

## Tag
Props:
- variant: 'default' | 'primary' | 'success' | 'warning' | 'error' | 'info' （默认 'default'）
- size: 'sm' | 'md' | 'lg' （默认 'md'）
- closable: boolean
- onClose: () => void
- className: string
- style: CSSProperties
- children: ReactNode
### Tag.Checkable
Props:
- checked: boolean （默认 false）
- onChange: (checked: boolean) => void
- value: string | number
- className: string
- style: CSSProperties
- children: ReactNode
### Tag.Group
Props:
- value: (string | number)[]
- defaultValue: (string | number)[]
- onChange: (value: (string | number)[]) => void
- className: string
- style: CSSProperties
- children: ReactNode
Usage:
import { Tag } from '@aura/ui';
<Tag variant="primary">标签</Tag>
<Tag.Checkable checked onChange={handleChange}>可选</Tag.Checkable>

## Badge
Props:
- count: number （默认 0）
- dot: boolean
- variant: 'default' | 'success' | 'warning' | 'error' | 'info' （默认 'error'）
- overflowCount: number （默认 99）
- showZero: boolean
- className: string
- style: CSSProperties
- children: ReactNode
Usage:
import { Badge } from '@aura/ui';
<Badge count={5}><Avatar>U</Avatar></Badge>
<Badge dot variant="success"><span>新消息</span></Badge>

## Avatar
Props:
- size: 'sm' | 'md' | 'lg' | number （默认 'md'）
- shape: 'circle' | 'square' （默认 'circle'）
- src: string
- alt: string
- variant: 'default' | 'primary' （默认 'default'）
- className: string
- style: CSSProperties
- children: ReactNode（src 加载失败时显示）
### Avatar.Group
Props:
- maxCount: number
- size: 'sm' | 'md' | 'lg' | number
- shape: 'circle' | 'square'
- className: string
- style: CSSProperties
- children: ReactNode
Usage:
import { Avatar } from '@aura/ui';
<Avatar src="/photo.jpg" alt="用户" />
<Avatar size="lg" variant="primary">A</Avatar>
<Avatar.Group maxCount={3}>
  <Avatar>A</Avatar><Avatar>B</Avatar><Avatar>C</Avatar><Avatar>D</Avatar>
</Avatar.Group>

## Tooltip
Props:
- content: ReactNode
- placement: 'top' | 'bottom' | 'left' | 'right' | 'topLeft' | 'topRight' | 'bottomLeft' | 'bottomRight' （默认 'top'）
- trigger: 'hover' | 'click' | 'focus' （默认 'hover'）
- delay: number （默认 0）
- disabled: boolean
- className: string
- style: CSSProperties
- children: ReactElement（仅接受单个 ReactElement）
Usage:
import { Tooltip } from '@aura/ui';
<Tooltip content="提示文字"><Button>悬浮</Button></Tooltip>

## Card
Props:
- variant: 'default' | 'elevated' | 'outlined' | 'glass' （默认 'default'）
- size: 'sm' | 'md' | 'lg' （默认 'md'）
- hoverable: boolean
- loading: boolean
- className: string
- style: CSSProperties
- children: ReactNode
子组件: Card.Header, Card.Title, Card.Body, Card.Actions, Card.Footer, Card.Cover
子组件 Props: className, style, children
Usage:
import { Card } from '@aura/ui';
<Card variant="elevated" hoverable>
  <Card.Header><Card.Title>标题</Card.Title></Card.Header>
  <Card.Body>内容</Card.Body>
  <Card.Footer>底部</Card.Footer>
</Card>
<Card loading />

## Collapse
Props:
- accordion: boolean （默认 false）
- defaultActiveKey: string | string[]
- activeKey: string | string[]
- onChange: (keys: string[]) => void
- className: string
- style: CSSProperties
- children: ReactNode
### Collapse.Item
Props:
- itemKey: string（必填）
- title: ReactNode
- disabled: boolean
- className: string
- style: CSSProperties
- children: ReactNode
Usage:
import { Collapse } from '@aura/ui';
<Collapse accordion>
  <Collapse.Item itemKey="1" title="面板一">内容一</Collapse.Item>
  <Collapse.Item itemKey="2" title="面板二">内容二</Collapse.Item>
</Collapse>

## Tabs
Props:
- defaultActiveKey: string
- activeKey: string
- variant: 'default' | 'card' | 'pill' （默认 'default'）
- size: 'sm' | 'md' | 'lg' （默认 'md'）
- onChange: (key: string) => void
- className: string
- style: CSSProperties
- children: ReactNode
### Tabs.Tab
Props:
- tabKey: string（必填）
- title: ReactNode
- disabled: boolean
- children: ReactNode
Usage:
import { Tabs } from '@aura/ui';
<Tabs defaultActiveKey="a" variant="card">
  <Tabs.Tab tabKey="a" title="标签A">内容A</Tabs.Tab>
  <Tabs.Tab tabKey="b" title="标签B">内容B</Tabs.Tab>
</Tabs>

## Empty
Props:
- description: ReactNode （默认 '暂无数据'）
- image: ReactNode（传 null 不显示）
- imageStyle: CSSProperties
- className: string
- style: CSSProperties
- children: ReactNode
### Empty.Preset
Props:
- type: 'noData' | 'noResult' | '404'
- className: string
- style: CSSProperties
- children: ReactNode
Usage:
import { Empty } from '@aura/ui';
<Empty description="暂无数据" />
<Empty.Preset type="404" />

## Message
静态方法（无需挂载组件）:
- message.success(content: string, duration?: number)
- message.error(content: string, duration?: number)
- message.warning(content: string, duration?: number)
- message.info(content: string, duration?: number)
- message.loading(content: string, duration?: number)
Usage:
import { message } from '@aura/ui';
message.success('操作成功');
message.loading('加载中', 0);

## Notification
静态方法（无需挂载组件）:
- notification.open(options: NotificationOptions)
- notification.success(options)
- notification.error(options)
- notification.warning(options)
- notification.info(options)
NotificationOptions:
- title: string
- content: string（必填）
- duration: number（默认 4.5）
- variant: 'default' | 'success' | 'warning' | 'error' | 'info'
- placement: 'topLeft' | 'topRight' | 'bottomLeft' | 'bottomRight'（默认 'topRight'）
- onClose: () => void
Usage:
import { notification } from '@aura/ui';
notification.success({ title: '成功', content: '操作完成' });
notification.error({ title: '错误', content: '出错了', placement: 'bottomRight' });

## Menu
Props:
- mode: 'vertical' | 'horizontal' | 'inline' （默认 'vertical'）
- selectedKey: string
- defaultSelectedKey: string
- onSelect: (key: string) => void
- collapsible: boolean
- className: string
- style: CSSProperties
- children: ReactNode
### Menu.Item
Props:
- itemKey: string（必填）
- disabled: boolean
- icon: ReactNode
- className: string
- style: CSSProperties
- children: ReactNode
### Menu.SubMenu
Props:
- subKey: string（必填）
- title: ReactNode
- icon: ReactNode
- className: string
- style: CSSProperties
- children: ReactNode
### Menu.Group
Props:
- title: ReactNode
- className: string
- style: CSSProperties
- children: ReactNode
Usage:
import { Menu } from '@aura/ui';
<Menu mode="vertical" defaultSelectedKey="home">
  <Menu.Item itemKey="home" icon={<HomeIcon />}>首页</Menu.Item>
  <Menu.SubMenu subKey="settings" title="设置">
    <Menu.Item itemKey="profile">个人资料</Menu.Item>
  </Menu.SubMenu>
</Menu>

## Breadcrumb
Props:
- separator: ReactNode （默认 '/'）
- className: string
- style: CSSProperties
- children: ReactNode
### Breadcrumb.Item
Props:
- href: string
- onClick: () => void
- className: string
- style: CSSProperties
- children: ReactNode
Usage:
import { Breadcrumb } from '@aura/ui';
<Breadcrumb>
  <Breadcrumb.Item href="/">首页</Breadcrumb.Item>
  <Breadcrumb.Item>当前页</Breadcrumb.Item>
</Breadcrumb>

## Pagination
Props:
- current: number
- defaultCurrent: number （默认 1）
- pageSize: number （默认 10）
- total: number（必填）
- showSizeChanger: boolean
- showQuickJumper: boolean
- size: 'sm' | 'md' （默认 'md'）
- onChange: (page: number, pageSize: number) => void
- className: string
- style: CSSProperties
Usage:
import { Pagination } from '@aura/ui';
<Pagination total={100} pageSize={10} onChange={handlePageChange} />
<Pagination total={500} showSizeChanger showQuickJumper />

## Steps
Props:
- current: number （默认 0）
- variant: 'default' | 'dot' | 'navigation' （默认 'default'）
- size: 'sm' | 'md' （默认 'md'）
- direction: 'horizontal' | 'vertical' （默认 'horizontal'）
- onChange: (current: number) => void
- className: string
- style: CSSProperties
- children: ReactNode
### Steps.Step
Props:
- title: ReactNode（必填）
- description: ReactNode
- icon: ReactNode
- disabled: boolean
- className: string
- style: CSSProperties
Usage:
import { Steps } from '@aura/ui';
<Steps current={1}>
  <Steps.Step title="步骤一" description="描述" />
  <Steps.Step title="步骤二" description="描述" />
  <Steps.Step title="步骤三" description="描述" />
</Steps>

## Dropdown
Props:
- menu: DropdownMenuItem[]（必填）
- trigger: 'hover' | 'click' （默认 'hover'）
- placement: 'bottomLeft' | 'bottomRight' | 'topLeft' | 'topRight' （默认 'bottomLeft'）
- disabled: boolean
- onMenuClick: (key: string) => void
- className: string
- style: CSSProperties
- children: ReactElement
DropdownMenuItem:
- key: string（必填）
- label: ReactNode（必填）
- disabled: boolean
- danger: boolean
Usage:
import { Dropdown } from '@aura/ui';
<Dropdown
  menu={[
    { key: 'edit', label: '编辑' },
    { key: 'delete', label: '删除', danger: true },
  ]}
  onMenuClick={handleClick}
>
  <Button>更多</Button>
</Dropdown>

## Slider
Props:
- min: number （默认 0）
- max: number （默认 100）
- step: number （默认 1）
- value: number | [number, number]
- defaultValue: number | [number, number] （默认 0）
- disabled: boolean
- marks: Record<number, ReactNode>
- range: boolean
- onChange: (value: number | [number, number]) => void
- className: string
- style: CSSProperties
Usage:
import { Slider } from '@aura/ui';
<Slider defaultValue={30} onChange={handleChange} />
<Slider range defaultValue={[20, 80]} />
<Slider marks={{ 0: '0', 50: '50', 100: '100' }} />

## Rate
Props:
- count: number （默认 5）
- value: number
- defaultValue: number （默认 0）
- allowHalf: boolean
- allowClear: boolean
- disabled: boolean
- size: 'sm' | 'md' | 'lg' （默认 'md'）
- onChange: (value: number) => void
- className: string
- style: CSSProperties
Usage:
import { Rate } from '@aura/ui';
<Rate onChange={handleChange} />
<Rate allowHalf allowClear defaultValue={3.5} />

## Upload
Props:
- accept: string
- multiple: boolean
- disabled: boolean
- maxSize: number（字节）
- listType: 'text' | 'picture' | 'picture-card' （默认 'text'）
- action: string
- headers: Record<string, string>
- onChange: (fileList: UploadFile[]) => void
- beforeUpload: (file: File) => boolean | Promise<File>
- className: string
- style: CSSProperties
UploadFile:
- uid: string
- name: string
- status: 'uploading' | 'done' | 'error'
- url?: string
- file?: File
### Upload.Dragger
继承 UploadProps，额外：
- children: ReactNode
Usage:
import { Upload } from '@aura/ui';
<Upload action="/api/upload" onChange={handleChange}>
  <Button>点击上传</Button>
</Upload>
<Upload.Dragger accept=".png,.jpg" maxSize={5 * 1024 * 1024}>
  拖拽文件到此处
</Upload.Dragger>

## Form
Props:
- layout: 'horizontal' | 'vertical' | 'inline' （默认 'vertical'）
- initialValues: Record<string, unknown>
- onFinish: (values: Record<string, unknown>) => void
- onFinishFailed: (errors: FormError[]) => void
- className: string
- style: CSSProperties
- children: ReactNode
### Form.Item
Props:
- name: string
- label: ReactNode
- rules: RuleType[]（required, message, pattern, validator）
- required: boolean
- className: string
- style: CSSProperties
- children: ReactNode
FormError: { name: string; errors: string[] }
Usage:
import { Form, Input, Button } from '@aura/ui';
<Form layout="vertical" initialValues={{ name: '' }} onFinish={handleSubmit}>
  <Form.Item name="name" label="姓名" rules={[{ required: true, message: '请输入姓名' }]}>
    <Input placeholder="请输入" />
  </Form.Item>
  <Button variant="primary" htmlType="submit">提交</Button>
</Form>

## Result
Props:
- variant: 'success' | 'error' | 'warning' | 'info' | '404' | '403' | '500'
- title: ReactNode（必填）
- subtitle: ReactNode
- icon: ReactNode
- extra: ReactNode
- className: string
- style: CSSProperties
Usage:
import { Result, Button } from '@aura/ui';
<Result variant="success" title="提交成功" subtitle="感谢您的反馈" extra={<Button>返回首页</Button>} />

## Popconfirm
Props:
- title: ReactNode（必填）
- description: ReactNode
- onConfirm: () => void
- onCancel: () => void
- okText: string （默认 '确定'）
- cancelText: string （默认 '取消'）
- variant: 'default' | 'warning' | 'error' （默认 'default'）
- placement: 'top' | 'bottom' | 'left' | 'right' （默认 'top'）
- disabled: boolean
- className: string
- style: CSSProperties
- children: ReactElement
Usage:
import { Popconfirm } from '@aura/ui';
<Popconfirm title="确定删除？" onConfirm={handleDelete} variant="error">
  <Button variant="primary">删除</Button>
</Popconfirm>

## Layout
Props:
- hasSider: boolean
- className: string
- style: CSSProperties
- children: ReactNode
子组件: Layout.Header, Layout.Body, Layout.Sider, Layout.Footer
### Layout.Header / Layout.Body / Layout.Footer
Props: className, style, children
### Layout.Sider
Props:
- width: number （默认 200）
- collapsible: boolean
- className, style, children
Usage:
import { Layout } from '@aura/ui';
<Layout hasSider>
  <Layout.Sider collapsible width={200}>
    <Menu>...</Menu>
  </Layout.Sider>
  <Layout>
    <Layout.Header>头部</Layout.Header>
    <Layout.Body>内容</Layout.Body>
    <Layout.Footer>底部</Layout.Footer>
  </Layout>
</Layout>

## Flex
Props:
- direction: 'row' | 'column' | 'row-reverse' | 'column-reverse' （默认 'row'）
- justify: 'start' | 'center' | 'end' | 'between' | 'around' | 'evenly'
- align: 'start' | 'center' | 'end' | 'stretch' | 'baseline'
- wrap: 'nowrap' | 'wrap' | 'wrap-reverse' （默认 'nowrap'）
- gap: 'sm' | 'md' | 'lg' | number
- className: string
- style: CSSProperties
- children: ReactNode
Usage:
import { Flex } from '@aura/ui';
<Flex justify="center" align="center" gap="md">
  <div>A</div>
  <div>B</div>
</Flex>

## Scrollbar
Props:
- maxHeight: number | string
- maxWidth: number | string
- alwaysShow: boolean （默认 false）
- className: string
- style: CSSProperties
- children: ReactNode
Usage:
import { Scrollbar } from '@aura/ui';
<Scrollbar maxHeight={300}>
  <div>长内容...</div>
</Scrollbar>
